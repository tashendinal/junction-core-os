[package]
name = "junction-node-agent"
version = "0.1.0"
edition = "2021"
description = "UDP cluster heartbeat broadcaster for Junction Core OS"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
